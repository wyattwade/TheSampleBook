#pins
  - @pictures.each do |picture|
    .box.panel.panel-default
      = link_to (image_tag picture.image.url), picture
      .panel-body
        %p.user
        Submitted by
        = picture.user.email

